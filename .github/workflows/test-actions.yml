name: Test actions

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Input test'
        required: true
        default: 'Hello'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Get env
      run: |
        echo "$ uname -a"
        uname -a
        echo "$ cat /etc/issue"
        cat /etc/issue
        echo "$ lscpu"
        lscpu
        echo "$ free -h"
        free -h
        echo "$ df -h"
        df -h
        echo "$ ifconfig"
        ifconfig
        echo "$ nv"
        env
        echo "$ id"
        id

        
    - uses: actions/checkout@v4

    - name: Build
      run: gcc ./src/main.c -o out

    - name: Test
      run: ./out # TODO
      
